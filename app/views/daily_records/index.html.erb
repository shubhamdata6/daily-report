<!DOCTYPE html>
<html>
<head>
  <title>Daily Record Report</title>
</head>
<body>
  <div class="container">
    <h1>Daily Record Report</h1>

    <table>
      <thead>
        <tr>
          <th>Date</th>
          <th>Male Avg Count</th>
          <th>Female Avg Count</th>
          <th>Male Avg Age</th>
          <th>Female Avg Age</th>
        </tr>
      </thead>
      <tbody>
        <% @daily_records.each do |record| %>
          <tr>
            <td><%= record.date %></td>
            <td><%= record.male_count %></td>
            <td><%= record.female_count %></td>
            <td><%= avg_round_off(record.male_avg_age) %></td>
            <td><%= avg_round_off(record.female_avg_age) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <!-- Pagination Links -->
    <div class="pagination">
      <%= paginate @daily_records %>
    </div>

    <div class="back-link">
      <%= link_to 'Back to Users', users_path, class: 'btn' %>
    </div>
  </div>
</body>
</html>
